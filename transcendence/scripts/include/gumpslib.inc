function CreateGump( pages := 1)

var gumpStruct := struct;
gumpStruct.+numPages;
gumpStruc.+background;
gumpStruct.+pages;
gumpstruct.+data;

gumpStruct.numPages := pages;
gumpStruct.background := {"page 0"};
gumpStruct.pages := {};
var i;
for(i := 1; i <= numpages; i := i + 1;)
gumpStruct.pages.append({"page " + cstr(i)});
endfor

return gumpStruct;

endfunction

///////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////////////

function AddPage( byref gump)

gump.numPages := gump.pages + 1;
gump.pages.append({"page " + cstr(gump.numPages)});


endfunction

///////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////////////

function NoMove(byref gump)
gump.background.append("nomove");
endfunction

///////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////////////

function NoClose(byref gump)
gump.background.append("noclose");
endfunction

///////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////////////

function NoDispose(byref gump)
gump.background.append("nodispose");
endfunction

///////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////////////

function SetBackgroundPicture(byref gump, gumpic)
gump.background.append("gumpic");
endfunction

///////////////////////////////////////////////////////////////////////////////////////
///////////////////////////////////////////////////////////////////////////////////////

function AddGumpPic(byref gump, gumpic, x, y, page)
if(page == 0)
gump.background.append("gumpic " + cstr(gumpic)+ " " + cstr(x) + " " + cstr(y));
else
gump.pages[page].append("gumpic " + cstr(gumpic)+ " " + cstr(x) + " " + cstr(y) );	
endif	
endfunction